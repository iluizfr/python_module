[tool.poetry]
name = "matrix-loading"
version = "0.1.0"
authors = ["luis"]

[tool.poetry.dependencies]
python = "^3.10"
numpy = "*"
pandas = "*"
matplotlib = "*"
requests = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.mansonry.api"
